# T012 — WIRING + KANIT · DONE

Her iki senaryo da **derlenmiş binary** üzerinden koşuyor. Fonksiyonları elle sıralayan bir test,
kırılan şeyi ölçemezdi: `init` ile `build` kendi testlerini ayrı ayrı geçiyordu ve yine de
birbirini reddediyorlardı.

## FR-015 · `init` → `build`
```
--- PASS: TestScaffoldBuildsEndToEnd (5.84s)
```
P0-2'nin şeklinin regresyon kapısı: iskelet, build kapısının reddettiği bir ağaç üretiyordu ve
ret "run `palbase init`" diyordu — bir çember. Çifti hiçbir şey ölçmüyordu.

## FR-016 · `start` → `/.well-known` → `stop`
```
--- PASS: TestStartServesAndStopCleansUp (53.10s)
```
**Compose düzeltmesinin tek kanıtı bu.** Vendor'lanan belge v0.52.0'da geçersizdi ve bir düzeltme
commit'inden sonra v0.53.0'da HÂLÂ geçersizdi; hiçbir test docker'ı ona karşı koşturmuyordu —
parite kapısı, ikisi de aynı bozuk fonksiyondan geçen iki dizeyi karşılaştırıyordu. 200 dönen bir
yığın, o kapının veremeyeceği cevap.

## Tam suite
`go build ./...` temiz · `go test ./... -count=1 -short` → **FAIL satırı 0**, real **96.93s** (bütçe 180s)
