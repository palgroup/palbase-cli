# T002 — e2e paketi derlenir · DONE

Kırmızı: `go vet -tags e2e ./tests/e2e/` → `too many arguments in call to auth.LoadDPoPKey / have (string) / want ()`

Plan bir bayat çağrı bekliyordu; **iki** çıktı:
1. `auth.LoadDPoPKey(mode)` → `auth.LoadDPoPKey()`; besleyen `PALBASE_MODE` bloğu da silindi (emekli v1 kablosu).
2. `transport.New(base, key, pat)` → `transport.New(base, pat)`; DPoP anahtarı artık istemciye geçmiyor,
   `transport.DPoPSigner` paket-düzeyi seam'ine bağlanıyor — `cmd/palbase`'in `wireDPoPSigner`'ı ile aynı şekil.

Yeşil: `go vet -tags e2e ./tests/e2e/` → çıktı yok, **exit 0**.
