# T008 — Android artefakt uyumu · DONE · **P0-5 KAPANDI**

## Üç uyumsuzluk
Eklenti DÜZ config + tek `.palbase/openapi.json` bekliyordu; CLI çok-ortam belge ve ortam
başına sözleşme yazıyor, üstelik bulut yolu eski tek dosyayı **siliyor**. `palbase android link`'in
ürettiği her artefakt okunamıyordu. Ayrıca eklenti `https` şart koşuyordu — `palbase start`
`http://127.0.0.1` yayımlıyor, yani sertifika verilemeyecek tek adres reddedilen adresti.

## Yön (D-4)
Uyum sağlayan taraf TÜKETİCİ. CLI dört platforma tek şekil yazar; eklenti ona uydu.

## Ölçüm dürüstlüğü
İlk "kırmızı" koşum ORTAM hatasıydı (`SDK location not found … ANDROID_HOME`) — kanıt
sayılmadı. SDK dışa aktarıldı, önce temel çizgi doğrulandı (değişikliklerim stash'te →
`BUILD SUCCESSFUL`), sonra ölçüldü.

## Mutation kanıtı
```
düzeltme geri alındı  → 12 tests completed, 2 failed  (tam olarak bu iki test)
düzeltme geri kondu   → BUILD SUCCESSFUL (12/12)
```
CLI tarafı: `go test ./internal/backend/ -run 'Android|Native'` → **ok**

Android commit: `c1918f4` (ayrı depo).
