# T003 — Lint borcu · internal/backend · DONE

**22 → 0 bulgu.** `GOTOOLCHAIN=go1.26.6 golangci-lint run ./internal/backend/...` → `0 issues.`

## Gerçek kusur (E-3) — `.env` mühür yazımı
`appendSealingChain` yazılan bir dosyada `defer f.Close()` kullanıyordu: kısa yazım ya da
flush hatası YALNIZCA `Close()`'ta görünür, yutulunca `.env` sessizce yarım mühürlü kalır.
Açık `closeErr()` ile değiştirildi, hata adıyla dönüyor.

## E-2'nin yarısı: ölü alanlar + onların vaadi
`deploy.go` `idempotencyKey` ve `uploadRetries` alanlarını BEYAN ediyor, yorumları davranışı
anlatıyor, `runPush`'un dokümanı "carrying an Idempotency-Key" diyordu — **hiçbiri okunmuyor**.
Alanlar silindi ve yorumlar bugünkü gerçeği söyleyecek şekilde düzeltildi: transport
`PostMultipart(..., idempotencyKey)` destekliyor, çağıran hiç geçmiyor, yani zaman aşımına
uğrayıp ASLINDA inen bir yükleme ikinci deploy olarak yeniden gönderiliyor.
Yeteneğin kendisi bu artımın FR'si değil → defter D-03 (aşağıda).

## Yöntem notu — sezgi iki kez yanlış yeri buldu
"Fonksiyon sonu = sütun 0'daki `}`" ve "sonraki üst-düzey bildirim" sezgilerinin İKİSİ de
ham TypeScript dizeleri yüzünden yanlış kesti (`}` ve `const REG = [];` sütun 0'da).
Go'nun kendi AST'siyle kesen küçük bir araç yazıldı; kesimler ondan sonra doğru.

## Kanıt
```
$ GOTOOLCHAIN=go1.26.6 golangci-lint run ./internal/backend/...
0 issues.
$ go test ./internal/backend/ -count=1 -short -skip TestStackImagesTrackTheCoreVersion
ok  	github.com/palgroup/palbase-cli/internal/backend	240.309s
```
`TestStackImagesTrackTheCoreVersion` ÖNCEDEN kırık (üst akış sürüklenmesi) — defter D-02.
