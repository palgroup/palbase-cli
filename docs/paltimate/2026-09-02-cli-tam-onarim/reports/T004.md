# T004 — Lint borcu · diğer paketler · DONE

**20 → 0 bulgu.** `golangci-lint` (auth, storage, egress, logs, project, cmd) → `0 issues.`

- `internal/project/gitroot.go` **tümüyle silindi** — üç bildirimin üçü de ölü.
  Planın uyardığı tuzak doğrulandı: `deploy.go`'daki `gitRunner` AYRI pakette AYRI bir tip, ona dokunulmadı.
- `config/*.ts` döneminin kalıntıları: `storage` 4 sembol · `egress` 3 regex · `logs.reverse` ·
  `auth_test` 7 (iki tip ve onların METOTLARI dahil — metotlar ada göre eşleşmediği için ayrıca kesildi).
- Silmenin yan etkisi olarak boşta kalan `regexp`/`fmt`/`strings` importları da kaldırıldı.
- staticcheck: `logs.go` ST1005 kapanış noktası · `doctor.go` QF1002 → `switch store` (etiketli).

```
$ GOTOOLCHAIN=go1.26.6 golangci-lint run ./internal/auth/... ./internal/storage/... \
    ./internal/egress/... ./internal/logs/... ./internal/project/... ./cmd/...
0 issues.
$ go test <aynı 6 paket> -count=1 -short
ok auth 0.895s · ok storage 1.612s · ok egress 1.030s · ok logs 1.967s · ok project 2.210s · ok cmd/palbase 2.693s
```
