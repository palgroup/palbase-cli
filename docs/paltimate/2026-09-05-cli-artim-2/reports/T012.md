# T012 — Rota literali ↔ sunucu rotası kapısı (Artım 1'in ertelenen 008'i)

## Neden bugün, Artım 1'de değil
Yazıldığı gün **kırmızı doğardı**: `selection/resolve.go:192` hâlâ `GET /api/v2/projects`'i
çağırıyordu — bulutun servis etmediği bir rota. Doğduğu gün kırmızı olan kapı, birinin kapattığı
kapıdır; o yüzden kendisini yeşil yapan işi (FR-015, T010) bekledi. Numarası `008` Artım 1'in
planında **boş bırakılmıştı**, böylece bekleyiş görünür kaldı.

## Kapının kendi iki kusuru — ikisi de kapı yazılırken bulundu
1. **Dekoratör regex'i seçenekli rotaları yutuyordu.** `@Get("/config", { auth: false })` gibi ikinci
   argümanlı her rota, `")` şartı yüzünden görünmüyordu. Sonuç: `/v1/cloud/me` ve `/v1/cloud/config`
   *"servis edilmiyor"* diye raporlandı — oysa ikisi de `cloud.controller.ts`'te (573, 588). **Kapı,
   ölçtüğünü sandığı şeyin üçte ikisini görmüyordu.**
2. **Ölçemediğini adlandırmıyordu.** `/v1/management/*` ve `/api/v2/*` başka düzlemlerde servis
   ediliyor ve buradan okunamıyor. Sessizce atlamak, kazanılmamış bir sessizlik raporlamaktır
   (Artım 1'in N-2 dersi) — 47 literal artık ADIYLA loglanıyor.

## Doğduğu gün yeşil, ve gerçekten ölçüyor
```
$ go test -run TestEveryCloudRouteLiteralIsServed → ok (not-served listesi BOŞ)
$ (var olmayan rota enjekte) → --- FAIL: internal/apikey/apikey.go:216 calls
                                /v1/cloud/projects/{ref}/there-is-no-such-route
$ (geri al) → ok
```

Parent ağaç yanında değilse SKIP eder — komşusu `TestTheVendoredComposeMatchesTheRepository` aynı
sebeple: bu pin yalnız iki ağacın yan yana olduğu yerde, yani rotaların düzenlendiği makinede
ölçülebilir.
DONE
