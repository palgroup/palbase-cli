# T010 — Seçim katmanını SÖK ve ölü rota çağrısını kaldır

## Sökülenler
- **`--project` / `--environment`** (`cmd/palbase/main.go`) — global override'lardı ve
  `GET /api/v2/projects` üzerinden çözülüyorlardı; v2 bulut o rotayı **servis etmiyor**. Yani
  belgelenmiş, kabul edilen ve 15+ komutta **hiçbir şey seçmeyen** bayraklardı.
- **`selection.ResolveProjectID`** — ölü rotayı çağıran fonksiyon; bayrak gidince çağıransız kaldı.
- **`Resolver.ProjectFlag` / `.EnvironmentFlag`** — `EnvironmentFlag` hâlâ `pickEnvironment`'a
  gidiyordu ama kimse doldurmuyordu: ölü kablo.
- **`projectIDPrefix`**, **`inScratchCheckout`** (cmd) — lint ölü olduklarını söyledi.

## İki bayat mesaj da düzeldi
`no project selected … or pass --project/--environment` ve `re-select with … --environment <ref>`
artık var olmayan bayrakları önermiyor. **Bir bayrağı kaldırmak, onu öneren cümleyi kaldırmaz** —
ikisi ayrı iş.

## Kapının kendi körlüğü
Yokluk testi ölü rotayı grep'lerken `transport/rest.go:180`'i yakaladı — orası bir **doküman
yorumu**, örnek olarak yazılmış bir yol. Düzyazıyı koddan ayıramayan bir kapı, var olmayan bir
kusuru rapor eder. Yorum satırları artık atlanıyor.

## Emekli edilen testler (hepsi gerekçesiyle)
`TestResolveProjectID_*` (3) · `TestStackVerbsRefuseTheEnvironmentFlag…` · `TestLogsRefuses…` ·
`TestResolve_ProjectFlagOverridesConfig` · `TestResolve_EnvironmentFlagMatchesExactSlugOrRef` ·
`TestResolve_EnvironmentDisplayNameIsNotASelectorAlias` · `TestResolve_EnvironmentFlagAloneNamesNoProject`
— hepsi artık var olmayan bir bayrağı ölçüyordu.

**Biri emekli EDİLMEDİ, dönüştürüldü:** `TestResolve_UnknownEnvironmentListsTheRealOnes`. Bayrak
üzerinden geliyordu ama ölçtüğü durum hâlâ gerçek — commit'li bir config, sonradan silinmiş bir
ortamı adlandırabilir. Config üzerinden kuruldu.

## `tests/e2e` KORUNDU (D-051)
Tasarım onu silinecekler arasında sayıyordu; Artım 1 o pakete bloklayıcı bir CI kapısı bağlamıştı.
`go vet -tags e2e ./tests/e2e/` → temiz.

## Ölçüm
```
$ go test ./... -short  → exit=0, 0 FAIL
$ golangci-lint run     → 0 issues.
$ go vet -tags e2e ./tests/e2e/ → temiz
```
DONE
