# T005 — `upgrade` yerel yığında ölü uç adlandırmayı bıraksın

## Kusurun şekli
`refFromTargetURL` loopback için `""` döndürüyordu — **doğru**, çünkü bir ref uydurmak yükseltmeyi
var olmayan bir proje id'sine nişanlardı. Yanlış olan SONRASIYDI: çağıran genel
*"no project to upgrade: run `palbase link <project>` first"* mesajına düşüyordu ve checkout
**linkliydi** — bu makinedeki bir yığına. Okuyucuya zaten yaptığı şeyi yapması söyleniyordu.

Bu dosyanın ilk testinin 2026-09-01'de kapattığı sınıfın aynısı, bir hedef-türü ötede.

## Adım 2: Kırmızıyı gör
```
$ go test ./internal/backend/ -run TestUpgradeOnALocalStack -count=1
internal/backend/upgrade_test.go:137:9: undefined: localStackUpgradeRefusal
```

## Adım 4: Yeşili gör
```
--- PASS: TestRefFromTargetURL
--- PASS: TestRefFromTargetURLRefusesWhatIsNotARef
--- PASS: TestUpgradeConfirmsAfterADroppedConnection
--- PASS: TestUpgradeOnALocalStackSaysWhatItIsAndWhatToDoInstead
ok  	github.com/palgroup/palbase-cli/internal/backend	0.425s
```

## Wiring
`upgrade.go:194` — `RunE` içinde, `refFromTargetURL`'den ÖNCE. Ret artık yön veriyor:
*"set stackVersion in .palbase/project.json and run `palbase start`"* — T001'in eklediği alanı
işaret ediyor, yani iki görev aynı yolu tarif ediyor.

DONE
