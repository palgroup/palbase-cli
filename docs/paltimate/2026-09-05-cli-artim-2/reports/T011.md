# T011 — github kolu ve `apps`'in ölü komut grubu SİLİNDİ

## Adım 1: Çağıranları ÖLÇ — ve tasarımın DÖRDÜNCÜ bayat iddiası çıktı

Tasarım "`internal/apps` (617) ve `internal/hook` (494) silinsin" diyordu. Ölçüm ikisinin de
tümüyle ölü OLMADIĞINI gösterdi:

| | Bulgu |
|---|---|
| `apps.Cmd` | `grep -rn apps.Cmd` → **sıfır çağrı**. Komut grubu ölü. |
| `apps`'in tipleri | `ConfigArtifact`, `ConfigArtifactPath`, `OAuthApple`, `OAuthConfig`, `OAuthGoogle`, `ValidateConfigArtifact` — **canlı**, `mobile_ios_capabilities.go` ve `cloud_environments.go` kullanıyor. |
| `internal/hook` | `doctor.go:193` ve `deploy.go`'nun CLONE yolundan çağrılıyor — **paket kalır**. |

Bir paketi "seçim katmanına dayanıyor" diye silmek, içindeki canlı yüzeyi de silmek olurdu. Kesim
**satır sayısını değil çağıranları izledi** (Changelog A-8).

## Silinenler
- **github `repository_provider` kolu** — `push`, `clone`, `pull`'daki üç dal, ve onlarla ölü kalan
  `requireMappedGitBranch` (lint söyledi).
- **`apps`'in komut grubu** — 618 → **152 satır**. `list/create/delete/enforce/attest/identifier/
  team-id/config` alt komutları hiçbir yerde mount edilmiyordu: kimsenin bağlamadığı bir komut
  grubu, sonraki okuyucunun *hiç koşmadığını* öğrenmeden önce anlamak zorunda olduğu 380 satırdır.
- **Yedi test** — github provider davranışını ölçüyorlardı; hepsi doğruydu ve hepsi artık
  mintlenmeyen bir provider hakkındaydı.

## Yerini alan kapı
`TestDeployVerbsHaveOneProviderPath` üretim kodunda `ProviderGitHub` grep'liyor — **bir kol bir
fiilden kesilip başka bir fiilde hayatta kalabilir.**

## Ölçüm
```
$ go test ./... -short → exit=0, 0 FAIL
$ golangci-lint run    → 0 issues.
```
DONE
