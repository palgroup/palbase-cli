# T013 — WIRING + KANIT: zincir üretim yolundan

## Neden bu görev var
Her görev kendi yarısını kanıtladı — sürüm alanı, imaj tablosu, algılayıcı, emekli komutlar, sökülen
seçim katmanı. **Hiçbiri kişinin sorduğu soruyu cevaplamıyor:** fiiller hâlâ birbirine bağlanıyor mu?
Ve o soru daha önce YANLIŞ cevaplandı — `palbase init` `palbase build`'in reddettiği bir ağaç kurdu,
ve **iki yarı da kendi testlerini geçiyordu**.

## Ölçülen (derlenmiş binary üzerinden)
- `init` → proje kuruluyor
- `link` → checkout'un NE olduğunu okuyor; backend checkout'unda **Apple iddia etmiyor** (eski
  davranış `--platform` varsayılanı `ios` olduğu için sessizce Apple artefaktı yazardı)
- `.palbase/project.json` → `stackVersion` taşıyor (türetildi ve YAZILDI)
- `start --help` → çözüm yolu ayakta
- `palbase ios link` · `web link` · `android use` → **"unknown command"**, shim yok
- `palbase --help` → `--project` / `--environment` YOK

## Sonuç
```
--- PASS: TestTheWholeChainFromInitToLink (4.43s)
$ go test ./... -short → exit=0, 0 FAIL
$ golangci-lint run    → 0 issues.
```

Yol boyunca bir artık bulundu ve düzeltildi: `start.go:1044`'te Artım 1'in N-3 temizliğinden kaçmış
bir ASCII Türkçe yorum satırı.
DONE
