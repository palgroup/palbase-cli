# T008 — Emekli komutları SÖK, shim bırakmadan

## Kapının kendisi ölçmüyordu — ve bunu ilk sürümü kanıtladı
İlk yazdığım yokluk testi yalnız `exit ≠ 0` istiyordu ve **komutlar hâlâ kayıtlıyken GEÇTİ**: yedisi
de kendi sebepleriyle (eksik kimlik, eksik argüman) zaten sıfırdan farklı çıkıyordu. Yani test tam
olarak kaldırmak için yazıldığı yüzeye karşı yeşildi.

Düzeltme: cobra'nın **"unknown command"** cümlesini şart koş. Başka bir sebeple düşen çağrı artık
"bu hiçbir şey kanıtlamıyor" diyerek testi düşürüyor.

## Sökülenler
- Kayıt: `backend.go`'dan `newWebCmd`, `newIOSCmd`, `newMacOSCmd`, `newAndroidCmd`.
- Dosyalar: `ios_use.go`, `android_link.go` (boşaldı), `android_use.go`, `web_use.go`,
  `native_use_test.go`, `web_use_test.go`, ve `newIOSLinkCmd` (lint ölü olduğunu söyledi).
- Grup testleri: `TestCommands_MacOSGroup/_AndroidGroup/_WebGroup` — ölçtükleri grup artık yok.
  Yerlerini alan test daha güçlü: GERÇEK binary'yi koşuyor.

## Yol boyunca çıkan iki gerçek
1. **`isolatedCheckout` silinen dosyada yaşıyordu** ama başka testler kullanıyordu — yardımcı, onu
   getiren yüzeyden uzun ömürlü çıktı. Korundu ve gerekçesi yazıldı.
2. **Çıplak `link` bağlı checkout'ta düşüyordu** — spec'in sınır durumu. `runLink` artık `--url`
   yoksa `Target`'tan okuyor: re-link, sözleşme değiştikten sonra istemciyi tazelemenin yolu ve
   adresi yeniden yazdırmak okuyucuya commit'li dosyanın söylediğini tekrarlatmaktır.

## Ölçüm
```
$ go test ./cmd/palbase/ -run TestRetiredPlatformCommands  → 7/7 PASS (gerçek binary)
$ go test ./... -short  → exit=0, 0 FAIL
$ golangci-lint run     → 0 issues.
```
DONE
